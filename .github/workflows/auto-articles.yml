name: 週次AI記事自動生成

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Japanese font (for OG image generation)
        run: sudo apt-get update && sudo apt-get install -y fonts-ipafont-gothic

      - name: Install dependencies
        run: pip install anthropic feedparser Pillow

      - name: Generate articles
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/generate_articles.py

      - name: Generate per-article OG images
        run: python scripts/generate_ogp.py

      - name: Regenerate sitemap
        run: python scripts/generate_sitemap.py

      - name: Commit and push if articles were generated
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add -A
          git diff --staged --quiet || git commit -m "自動更新: AI最新記事 $(TZ=Asia/Tokyo date +'%Y/%m/%d')"
          git push
