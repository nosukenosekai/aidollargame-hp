# メモ：スライド・図版のデザインを底上げする作り方（2026-08-18）

> きっかけ：「毎回デザインが下手」という自覚があるので、これを機にやり方を変える。
> 出典：X の @RealtyPnw（Tak）のポスト（2026-08-18 12:56・21万表示）と、その返信スレッド。

## スレッドの要点

1. **Tak（元ポスト）**
   これまでは ChatGPT に HTML かパワポでスライドを作らせていたが、**Figma を直接いじらせると圧倒的に作りやすい**。もっと早くやればよかった。

2. **Tak（補足）**
   ただし **MCP コールはすぐ Figma の無料利用リミットに達する**。コツは **Figma をブラウザ上で Codex に操作してもらうこと**。

3. **Naoko Manchun Milky Naomi（返信）**
   自分は **Markdown で書かせて → Marp で HTML を吐かせる**のを Claude Code 上でやっている。Figma は MCP が重い印象。ただしエンジニア向けではなく **fancy なプレゼン**を作るなら話は別かもしれない。

4. **Tak（返信）**
   MCP が重い（Figma もいずれ課金が必要になる）ので、**Codex にブラウザ上で SVG を直接いじってもらっている**。自分もずっと Markdown → HTML だったが、それだと**「ここだけ変えて」の指示のはずが Claude / Codex ともに結構よけいな所まで変えてくる**。ブラウザ操作なら**指示が一番的確に処理される**し、スライドとしての見栄えもとてもいい。

## ここから学ぶこと（自分向けの結論）

- **「テキストで一括生成 → 全部作り直し」がデザインが下手に見える原因**。部分修正が効かないので、直すたびに全体が崩れる。
- 対策は**「見た目そのものを直接いじれる形式」で持つこと**。具体的には次の2択。
  - **A. SVG を直接編集**：スライド1枚 = 1 SVG。座標・色・フォントサイズだけをピンポイントで書き換えられるので、「ここだけ変えて」が本当に「ここだけ」で済む。課金もツール依存もなし。
  - **B. Figma をブラウザで操作**：見栄え重視の営業資料向け。ただし **Figma MCP は重い＆無料枠をすぐ食う**ので、MCP ではなくブラウザ操作で触らせる。
- **Marp（Markdown → HTML/PDF）は「速いが凡庸」**。社内メモや手順書には十分だが、**お客様に出す提案資料には弱い**。使い分ける。
- 迷ったら：**中身を固める段階は Markdown、見せる段階は SVG**。

## このリポでの当面の運用

- 提案資料・セミナー資料・記事内の図版は、**まず SVG で1枚ずつ作る**（`articles/` の既存ページも SVG を使っているので相性が良い）。
- 修正依頼は「**この要素のこの属性だけ**」の粒度で出す。作り直しをさせない。
- PDF が必要なものは、SVG → HTML にまとめてブラウザ印刷で PDF 化（`shiryo/` の配布物と同じ流れ）。
- Figma を使うのは、**外に出す見栄え重視の資料に限定**。使うときも MCP ではなくブラウザ操作。

## 追記（2026-08-19）：資料は毎回 Figma を参考にしてから作る

本人指示：**「これから資料を作るときは毎回 Figma を参考にして」**。運用ルールとして `CLAUDE.md` に反映済み。

背景として整理したこと：

- **Figma がオシャレなのではなく、Figma に乗っているものがオシャレ。** 効いているのは主に**コミュニティのテンプレート（＝プロが作った型）**で、オートレイアウトやマウス操作は作業効率の話。
- **Figma の書き出しも結局 SVG。** 表現力の差ではないので、型さえ借りれば SVG 制作でも同じ見栄えになる。
- 「毎回デザインが下手」の正体は、**毎回ゼロから配色とフォントサイズを決め直していること**。型を固定すれば構造的に起きなくなる。
- スレッドの Tak 本人も、最初は「Figma 直接」と言いつつ、最後の返信では **MCP の重さと課金を理由に「ブラウザ上で SVG を直接いじる」に着地**している。参考にするのは Figma、作るのは SVG、という切り分けはこれと同じ。

制約：**Claude はこのリモート環境から Figma にログインできない。** よって分担は「**参考テンプレを選ぶ／スクショを渡すのは佐藤さん、SVG に落として作り込むのは Claude**」。

## 追記2（2026-08-19）：Figmaは見に行けない。型は自社サイトから取れた

- 確認結果：**このリモート環境からは `www.figma.com` も `api.figma.com` も 403（ネットワークポリシーで遮断）**。Claude が自分で Figma を見ることはできない。Figma MCP コネクタも未接続。
- 「毎回スクショを貼る」運用は続かない。**そもそも型は一度決めれば終わり**で、毎回参照する必要がない。
- そして**参考にすべき型はすでに自社サイトにあった**。`index.html` の `:root` にフォント・配色・角丸・影が定義済み。Figma のテンプレートを借りるまでもなかった。
- よって `content/slides/` に **SVGスライドテンプレート3枚（表紙・本文・比較）** を作成し、これを型として固定。以降はコピーして `data-edit` の中身を差し替えるだけ。
- Figma を参考にするのは「今の型に飽きた／別路線を試したい」時だけ。その時はユーザーがスクショを渡す。

## 追記3（2026-08-19）：Figmaアカウントを開設。Educationで無料

- アカウント：`nosuke` ／ チーム名「佐藤徳之介のチーム」。
- **Figmaエデュケーションの利用資格が認定された** → **Professionalプランの全特典が無料**。
  Tak が言っていた「Figma もいずれ課金が必要になる」という懸念は、**当面あてはまらない**。
- 初期状態で置かれている `FigJam basics` ／ `Figma basics` ／ `team library` は**Figma側が用意したチュートリアル用サンプル**。自分で作ったものではないので触らなくてよい。
- **Figma の操作を覚える必要はない。** 使うのは次の2箇所だけ。
  - **左メニュー「コミュニティ」** … 公開テンプレートを探す。参考にしたいものが見つかったらスクショを Claude に渡す。
  - **上部「Slides」** … Figma版のプレゼンツール。パワポに近く、組み込みテンプレートから始められる。
- 制作の本線は変わらず `content/slides/` の SVG テンプレート。Figma は**参考を探す場所**として使う。

## 追記4（2026-08-19）：なぜ「Claudeがログインすれば解決」にならないか

実測した結果、**壁は認証ではなくネットワーク**だった。

| 試したこと | 結果 |
|---|---|
| Web検索でテンプレ名・URLを探す | ✅ できる（サーバー側の検索なので制限を受けない） |
| `www.figma.com` / `api.figma.com` を開く | ❌ 403（egress proxy のポリシー拒否） |
| `google.com` / `dribbble.com` など画像ホスト | ❌ 同様にブロック |
| `github.com`・パッケージ管理系 | ✅ 通る |

つまり claude.ai/code のこの環境は**GitHubとパッケージ管理系以外、外に出られない**。
ログインは「あなたは誰か」を解く鍵で、ここで閉じているのは「そもそも到達できるか」。**別の壁なので鍵では開かない。**

**パスワードは渡さない／受け取らない。** 仮に渡されても上記の理由で使えないし、預かるべきものでもない。

到達可能にしたい場合の正攻法は次の2つ：

1. **環境のネットワークポリシーで `figma.com` を許可する**（claude.ai/code の環境設定）。
   その上で **Figma のパーソナルアクセストークン**（パスワードではない・スコープ付き・いつでも失効させられる）を渡せば、REST API 経由でファイルを読める。
   ただし **Community のファイルは API から直接は読めない**。いったん自分のドラフトに複製してからでないと対象にならない。
2. **Mac でローカルの Claude Code を動かす。** ネットワーク制限がないので、ブラウザ操作で直接見に行ける。Tak が Codex でやっているのはこの形。

当面は「Claude がリンクを出す → 佐藤さんが開いて選ぶ → スクショか判断を渡す」が最短。

## 追記5（2026-08-19）：本人の要望 ―「オシャレなデザイン」「UI」「パワポ資料」も欲しい

本人の言葉：**「めっちゃオシャレなデザインも欲しい。UIも。パワポ資料も」**

現時点では**要望として記録するだけ**。着手は別途指示を待つ。3つは別物なので、必要になったら下の区別で進める。

### 1. オシャレなスライド

- 今の `content/slides/` の型は**わざと「堅実」寄り**（明るい背景・青・落ち着いた余白）にしてある。会社案内・提案書向け。
- 本人が「オシャレ」と言っているのは、Figmaで見ていた **Free Grey Pitch Deck** 系統。
  **黒背景・特大の見出し・差し色1色（オレンジ）・数字を大きく見せる**路線。
- 対応：**堅実版を消さず、濃い版（dark）のテンプレを別途追加する。** 用途で使い分ける。
  - 堅実版 … 提案書・見積・行政や金融が相手
  - 濃い版 … セミナー・登壇・対外プレゼン

### 2. UI デザイン

- アプリ・デモ画面のモック。`index.html` の `:root` トークンがそのまま使えるので、配色とフォントは流用できる。
- **何の画面を作るかが決まっていない。** 着手前に対象を確認すること（既存の `demo/` 配下の画面か、新規か）。

### 3. パワポ資料（.pptx）

- **ここだけ技術的に別物。** SVG では `.pptx` にならない。
- 判断基準：
  - **相手に編集させる必要がある** → `.pptx` で書き出す必要あり（生成は可能）
  - **見てもらう・配るだけ** → PDF で足りる。SVG → HTML → 印刷で出せる
- 「パワポで」と言われたら、まず**どちらなのかを確認**する。編集不要なら PDF の方が崩れない。

## 次にやること（未着手）

- [x] スライドの SVG テンプレート（16:9・表紙／本文／比較）を作る → `content/slides/`
- [ ] 実際の提案資料を1本この型で作って、これまでのものと見比べる
- [ ] 濃い版（dark）のスライドテンプレートを追加する ← 「オシャレなのが欲しい」への対応
- [ ] `.pptx` 書き出しが要るか確認する（編集させるなら必要／配るだけならPDF）
- [ ] UIモックの対象画面を決める
- [ ] **Mac でローカルの Claude Code を立てる**（Figma を Claude 自身が見に行けるようにする）→ 手順は `content/memo/2026-08-19-local-claude-code.md`
- [ ] 既存の提案資料のうち1本を、この作り方で作り直して見比べる

---

## 2026-08-19 追記：Figmaを実測して、型に反映した

Mac のローカル Claude Code から Chrome を動かして、Figma コミュニティの**無料**プレゼンテンプレート3本を開き、右パネル（位置・サイズ・タイポグラフィ・選択範囲の色）で実測した。感想ではなく数値だけを取った。

| # | テンプレート | 作者 | ファイルキー |
|---|---|---|---|
| A | Free Grey Pitch Deck Presentation Template | Sanmi Sway I | `1264204971552949338` |
| B | Business Presentation Figma Template | BRIX Templates | `1175116565554028977` |
| C | Creatable Investment Pitch Deck | sarah | `1212523437031154982` |

3本とも 1920×1080。

### 実測値

| 項目 | A | B | C | 自社（変更前） |
|---|---|---|---|---|
| 左マージン（本文） | 104px | 106px | 120px | 120px |
| 見出し | 69 | 88 | 90 | 60 |
| 本文 | 24 | 24 | 26 | 28〜30 |
| 見出し÷本文 | 2.9倍 | 3.7倍 | 3.5倍 | **2.1倍** |
| 表紙タイトル | 281 | 116 | 130 | 96 |
| 表紙タイトルの縦位置 | Y=415 | Y=446 | Y=555 | Y=600 |
| 本文の文字色 | `#979797` | `#615E83` | `#595959` | `#3a3f4d`（比較は`#14161f`） |

配色：
- A … `#000000` / `#FFFFFF` / グレー4段（`#7F7F7F` `#D9D9D9` `#979797` `#606060`）／ 暖色 `#F25037` `#FE3E01` `#FF4602` ＋ `#FEBE03`
- B … 主役 `#4A3AFF` 1色 ＋ ニュートラル8段（`#0D0A2C` `#615E83` `#807E9A` `#E5E5EF` `#F2F2FF` `#F7F7FB` `#F8F8FF` `#FFFFFF`）＋ パープル8段（`#0B0086` `#0F00B3` `#3B28EB` `#766AFF` `#A9A2FF` `#C8C3FF` `#E1D8FF` `#F0EEFF`）
- C … `#191819` / `#FFFFFF` / `#F1F1F1` / `#595959` / `#8C8C8C` ＋ 増減を表す緑赤2組（`#61C554` `#CCE8C2` / `#ED6A5E` `#F8B2B2`）

### 型に反映した7点（`content/slides/`）

1. **本文をグレーに。** 3本とも本文はグレーで、黒は見出しだけ。`template-compare.svg` の導入後の本文が `#14161f` だったので `#3a3f4d` に。
2. **見出し 60 → 88。** 本文30に対して2.9倍。3本のレンジ（2.9〜3.7倍）に入れた。表紙タイトルも 96 → 116。
3. **赤い短い罫を小見出しの下へ。** これはAだけがやっている手。本文テンプレでは見出しの下から `box-label`（ここが効く）の下に移した。比較テンプレは矢印が赤なので罫を削除（これで「赤は1枚に1回」が3枚とも守られる）。
4. **左マージン120pxは据え置き。** Cと完全一致、A・Bとも近い。
5. **表紙タイトルを下半分へ。** 1行目のベースラインを 600 → 624、上に置くのはブランドマークだけにした。
6. **右端に幅71pxの縦帯**（`x=1849〜1920`）を新設し、縦組みで「社外秘 ／ 年月」。Cの構造そのまま。3枚すべてに入れた。
7. **青を5段のランプに。** `#213c8d` / `#2f56c9` / `#97abe4` / `#cdd6f2` / `#eef2fc`。表紙が `#c9d7f7` `#c7d2ee` `#7da4d2` と場当たりの青を3つ持っていたので、ランプの値に置き換えた。表やグラフで色数が要るときは別の色相を足さずにここから取る。

`content/slides/preview.html` をブラウザで開くと3枚まとめて確認できる。

### 手順として残しておくこと

- Figma のコミュニティファイルの **「Figmaで開く」は、押した時点で自分のドラフトに複製が作られる**（読み取り専用プレビューではない）。調べ終わったらドラフトを掃除する。
- 数値を一気に取るなら **右パネル最下部の「選択範囲の色」** が速い。全フレームを選んで開くと、そのファイルで実際に使われている色が使用頻度順に出る。
- 文字は **テキストスタイルの一覧**（右パネルの「スタイル」）を見れば、H1〜H6・Body の段が一度に読める。Bはスタイルガイドのページも持っていた。
